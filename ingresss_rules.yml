apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prom-grafana-ing
  namespace: ibm-ucd
spec:
  ingressClassName: nginx
  rules:
    - host: prm.cluster4live.com
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: prometheus-server
                port:
                  number: 80
            path: /
    - host: mgr.cluster4live.com
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: prometheus-alertmanager
                port:
                  number: 80
            path: /
    - host: gra.cluster4live.com
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 80
            path: /
